import { RpcValidationPipePipe } from './rpc-validation-pipe.pipe';

describe('RpcValidationPipePipe', () => {
  it('should be defined', () => {
    expect(new RpcValidationPipePipe()).toBeDefined();
  });
});
